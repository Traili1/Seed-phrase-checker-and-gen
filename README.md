# Seed-phrase-checker-and-gen
A BIP39 seed prase gen and checker open source
easy to use
Before using pls open command line and install these here
Pip install random
pip install threading
then it should work 
i already gave and words.txt bip39
